version: "3.1"

services:
  integration-tests:
    build:
      context: .
      dockerfile: ./integration-tests/Dockerfile
    hostname: integration-tests
    depends_on:
      - cart
      - loms
#    environment:
#      BASE_URL: "${BASE_URL}"
    command: ["go", "test", "-v", "./..."]
